__pycache__/
*.pyc
.cache/
episodes/
# audition candidates are disposable; the chosen cast WAVs ARE committed
# (voice design is nondeterministic — losing them would change the podcast's
# voices forever)
assets/voices/candidates/
assets/music/rendered/
*.egg-info/
build/
dist/

# Source scans pulled from archive.org for the lectionary migration —
# large, re-fetchable, and not ours to redistribute. See
# docs/lectionary-migration/README.md for the fetch commands.
*.jp2.zip
csb_jp2.zip
*_djvu.txt
scans/
